{
  "name": "friends",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "run": "node server.js",
    "build": "tsc && vite build && cp CNAME docs && cp .nojekyll docs",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "devDependencies": {
    "@babylonjs/materials": "^5.28.0",
    "@originjs/vite-plugin-commonjs": "^1.0.3",
    "typescript": "^4.6.2",
    "vite": "^2.8.6",
    "vite-plugin-chunk-split": "^0.4.1"
  },
  "dependencies": {
    "@babylonjs/core": "^6.2.0",
    "@babylonjs/gui": "^6.2.0",
    "express": "^4.18.2"
  }
}
