{
  "name": "friends",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "run": "node server.js",
    "build": "tsc && vite build && cp -rf dist/ ../Glitch/baby-webxr/ts",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "devDependencies": {
    "@babylonjs/materials": "^5.28.0",
    "@types/cannon": "^0.1.8",
    "typescript": "^4.6.2",
    "vite": "^2.8.6"
  },
  "dependencies": {
    "@babylonjs/core": "^5.0.0",
    "cannon": "^0.6.2",
    "express": "^4.18.2"
  }
}